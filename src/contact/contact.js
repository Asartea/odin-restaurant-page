import append from "../functions/append";

const contact = () => {
  const placeHolder = document.createElement("div");
  placeHolder.textContent = "Contact - WIP";
  append(placeHolder);
};

export default contact;
